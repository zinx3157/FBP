name: Deprecated — LabelOnZeWay legacy APK builder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This legacy patch-and-build workflow is intentionally retired."
          echo "Production runtime is maintained by the audited canonical LabelOnZeWay source."
          echo "Use the final production convergence workflow for source validation and the persistent local production signer for release APKs."
